[property]
enable=1
    # dummy unique id; we don't actually feed a downstream GIE
target-unique-ids=1
    # 0=NCHW (required in DS 8.0)
network-input-order=0
    # 1 = process on frames (0 = on objects)
process-on-frame=1
unique-id=5
gpu-id=0
maintain-aspect-ratio=0
symmetric-padding=0
processing-width=1280
processing-height=720
scaling-buf-pool-size=6
tensor-buf-pool-size=6
    # N;C;H;W — tensor we would emit if we were doing inference
network-input-shape=1;3;720;1280
    # 0=RGB 1=BGR 2=GRAY
network-color-format=0
    # 0=FP32 1=UINT8 2=INT8 3=UINT32 4=INT32 5=FP16
tensor-data-type=1
tensor-name=input
scaling-pool-memory-type=0
scaling-pool-compute-hw=0
scaling-filter=0
custom-lib-path=/opt/ramp-motion/libramp_motion_preproc.so
custom-tensor-preparation-function=CustomTensorPreparation

[user-configs]
pixel-normalization-factor=0.003921568

    # Groups are populated at runtime from config.yaml by ramp_motion/app.py:
    # one [group-N] per source with src-ids=N and roi-params-src-N=x;y;w;h.
