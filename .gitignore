__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
*.egg-info/
dist/
build/
preprocess/build/
data/
*.so
.venv/
.env

# Vendored OpenCV zips (user downloads for offline builds)
vendor/*.zip

# Local test/override configs (can contain real RTSP URLs, NEVER commit)
config.test.yaml
config.*.local.yaml
